package com.hermes.presentation.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Hermes App Color Palette
 * 深色主题科幻风格，基于原型设计规范
 */
object HermesColors {
    // Primary Colors
    val Primary = Color(0xFF3b82f6)      // 霓虹蓝
    val Secondary = Color(0xFFa855f7)    // 极光紫
    val Accent = Color(0xFF22d3ee)       // 强调色

    // Background Colors
    val Background = Color(0xFF020617)   // 最深背景
    val Surface = Color(0xFF0f172a)      // 卡片背景
    val SurfaceLight = Color(0xFF1e293b) // 浅卡片

    // Status Colors
    val Success = Color(0xFF22c55e)      // 成功/活跃
    val Warning = Color(0xFFeab308)      // 警告/待处理
    val Danger = Color(0xFFef4444)       // 危险/停用
    val Info = Color(0xFF12b7f5)         // 信息

    // Text Colors
    val TextPrimary = Color(0xFFf8fafc)  // 主要文本（白）
    val TextSecondary = Color(0xFF6b7280) // 次要文本
    val TextMuted = Color(0xFF64748b)    // 淡化文本

    // Identifier Type Colors
    val PhoneColor = Color(0xFF07c160)   // 手机号（微信绿）
    val EmailColor = Color(0xFF1677ff)   // 邮箱（蓝色）

    // Warning Level Colors
    val WarningHigh = Color(0xFFe53935)
    val WarningMedium = Color(0xFFeab308)
    val WarningLow = Color(0xFF22c55e)

    // Misc
    val Divider = Color(0xFF333333)
    val CardBorder = Color(0xFF1e293b)
}